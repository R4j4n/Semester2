import pandas as pd
import plotly.graph_objs as go
from plotly.subplots import make_subplots
from sklearn.linear_model import LinearRegression


class Visualizer:
    def __init__(
        self,data: pd.DataFrame,producer_name: str,movie_name: str,start_date: str,
        end_date: str,
        fig_height: int = 800,
    ) -> None:

    def plot_o_h_l_c_v(self):
        """This method plots the open, high, low, close of the given data."""

    def plot_relase_date(self, release_date):
        """Plot the data with highlight on relase date"

    def plot_overall_trends(self, release_date):
        """Plot the data with overall trend regressor"


 
